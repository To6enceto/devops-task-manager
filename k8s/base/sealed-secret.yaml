apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: task-manager-db-secret
  namespace: task-manager
spec:
  encryptedData:
    # This is a PLACEHOLDER. Generate real sealed secret with:
    #   kubectl create secret generic task-manager-db-secret \
    #     --namespace task-manager \
    #     --from-literal=DATABASE_PASSWORD=<your-password> \
    #     --dry-run=client -o yaml | kubeseal --format yaml > sealed-secret.yaml
    DATABASE_PASSWORD: AgByCmF...PLACEHOLDER...==
  template:
    metadata:
      name: task-manager-db-secret
      namespace: task-manager
    type: Opaque
